# 프로젝트 컨텍스트 (CLAUDE.md)

> 이 파일은 Claude Code가 프로젝트를 이해하고 일관된 작업을 수행하도록 돕습니다.
> 프로젝트 초기화 시 이 템플릿을 복사하여 프로젝트 루트에 배치하세요.

---

## 프로젝트 개요

- **이름**: [프로젝트명]
- **설명**: [한 줄 설명]
- **타입**: [웹앱 / API / 라이브러리 / CLI / 모바일]
- **주요 언어**: [TypeScript / Python / Go / etc.]
- **프레임워크**: [React / Next.js / FastAPI / etc.]

---

## 프로젝트 목표 (직접 작성)

> 이 섹션은 프로젝트마다 다르게 작성합니다.

### 비전
[프로젝트가 해결하려는 문제와 목표를 한 문단으로]

### 핵심 기능
1. [기능 1]
2. [기능 2]
3. [기능 3]

### 비기능 요구사항
- 성능: [예: 응답시간 200ms 이하]
- 가용성: [예: 99.9% 업타임]
- 보안: [예: OWASP Top 10 대응]

---

## 프로젝트 특화 규칙

> 기본 규칙(SSOT, MCP 활용, 기획 확인, 코드 품질)은 자동 적용됩니다.
> 이 프로젝트에만 적용되는 추가 규칙을 작성하세요.

```
[프로젝트 특화 규칙 예시]
- API 응답은 반드시 표준 응답 형식 사용
- 모든 컴포넌트는 Storybook 문서화 필수
- DB 쿼리는 ORM만 사용 (raw query 금지)
```

---

## 🚨 필수 규칙 (MUST)

> Claude는 아래 규칙을 **반드시** 준수해야 합니다.

### 1. 파일/폴더 구조 규칙

**파일 생성 전 반드시 `.claude/project-structure.yaml` 확인**

```
✅ 허용:
- src/features/[기능명]/     → 기능 코드
- src/entities/[엔티티명]/   → 비즈니스 엔티티
- src/shared/               → 공유 코드
- docs/[카테고리]/          → 문서

❌ 금지:
- src/ 내부에 .md 파일 생성
- 루트에 임의 폴더 생성
- temp*, backup*, *_old 파일
- notes.md, TODO.md 등 산발적 문서
```

**레이어 의존성 규칙** (상위 → 하위만 참조 가능):
```
app → pages → widgets → features → entities → shared
```

### 2. 문서 작성 규칙

**문서 생성 시 반드시 올바른 위치에 배치:**

| 문서 유형 | 위치 | 예시 |
|----------|------|------|
| 아키텍처/설계 | `docs/architecture/` | 시스템 구조, ERD |
| API 문서 | `docs/api/` | 엔드포인트 명세 |
| 가이드 | `docs/guides/` | 설치, 사용법 |
| 기술 결정 | `docs/decisions/` | ADR |
| 참고 자료 | `docs/references/` | 외부 링크 정리 |

**문서 생성 후 반드시:**
1. `docs/README.md` 인덱스 업데이트
2. 관련 문서에 상호 링크 추가

**루트에 허용되는 문서:**
- README.md, CHANGELOG.md, CONTRIBUTING.md, LICENSE

### 3. 테스트 코드 규칙

**테스트 유형별 위치:**

| 유형 | 위치 | 보존 |
|------|------|------|
| 단위 테스트 | `tests/unit/` | 영구 보존 |
| 통합 테스트 | `tests/integration/` | 영구 보존 |
| E2E 테스트 | `tests/e2e/` | 영구 보존 |
| 임시 테스트 | `tests/scratch/` | **PR 머지 전 삭제** |

**테스트 헬퍼 (재사용):**
- `tests/__helpers__/fixtures/` - 테스트 데이터
- `tests/__helpers__/mocks/` - 목 객체
- `tests/__helpers__/factories/` - 팩토리 함수

**⚠️ 임시 테스트 (tests/scratch/):**
- 디버깅/실험용으로만 사용
- 7일 이상 유지 시 경고
- PR 머지 전 반드시 삭제 또는 정식 테스트로 이동

### 4. 코드 작성 규칙

**네이밍 규칙:**
```
컴포넌트: PascalCase     → Button.tsx, UserProfile.tsx
훅:       camelCase      → useAuth.ts, useFetch.ts
유틸:     camelCase      → formatDate.ts
상수:     SCREAMING_SNAKE → API_ENDPOINTS.ts
폴더:     kebab-case      → user-profile/, add-to-cart/
```

**금지 사항:**
- 하드코딩된 시크릿/API 키
- console.log 남기기 (디버깅용)
- any 타입 남용
- 주석 처리된 코드
- 미사용 import/변수

---

## 프로젝트 구조

```
[프로젝트명]/
├── src/
│   ├── app/              # 앱 초기화, 라우팅
│   ├── pages/            # 페이지 컴포넌트
│   ├── widgets/          # 독립 UI 블록
│   ├── features/         # 기능 단위
│   ├── entities/         # 비즈니스 엔티티
│   ├── shared/           # 공유 리소스
│   └── infrastructure/   # 외부 연동
│
├── docs/
│   ├── README.md         # 문서 인덱스
│   ├── architecture/     # 설계 문서
│   ├── api/              # API 문서
│   ├── guides/           # 가이드
│   └── decisions/        # ADR
│
├── tests/
│   ├── unit/             # 단위 테스트
│   ├── integration/      # 통합 테스트
│   ├── e2e/              # E2E 테스트
│   ├── scratch/          # 임시 테스트 (삭제 대상)
│   └── __helpers__/      # 테스트 유틸리티
│
├── config/               # 환경별 설정
├── scripts/              # 빌드/배포 스크립트
│
├── .claude/
│   └── project-structure.yaml
│
├── README.md
├── CHANGELOG.md
└── package.json
```

---

## 기술 스택

| 구분 | 기술 |
|------|------|
| 언어 | [TypeScript 5.x] |
| 프레임워크 | [Next.js 14] |
| 스타일 | [Tailwind CSS] |
| 상태 관리 | [Zustand / React Query] |
| 테스트 | [Vitest / Playwright] |
| 린트/포맷 | [ESLint / Prettier] |

---

## 주요 명령어

```bash
# 개발
npm run dev          # 개발 서버
npm run build        # 빌드
npm run lint         # 린트
npm run format       # 포맷팅

# 테스트
npm run test         # 단위 테스트
npm run test:e2e     # E2E 테스트
npm run test:coverage # 커버리지

# 기타
npm run db:migrate   # DB 마이그레이션
npm run db:seed      # 시드 데이터
```

---

## 환경 변수

| 변수 | 설명 | 예시 |
|------|------|------|
| `DATABASE_URL` | DB 연결 문자열 | `postgresql://...` |
| `API_BASE_URL` | API 서버 URL | `https://api.example.com` |
| `JWT_SECRET` | JWT 시크릿 | (비공개) |

---

## 코드 컨벤션

### 커밋 메시지
```
<type>(<scope>): <description>

type: feat | fix | docs | style | refactor | test | chore
scope: 변경 범위 (선택)
description: 변경 내용 (현재형)

예: feat(auth): add JWT refresh token logic
```

### 브랜치 전략
```
main        ← 프로덕션
├── develop ← 개발 통합
│   ├── feature/[기능명]
│   ├── fix/[버그명]
│   └── refactor/[대상]
```

### PR 규칙
- 최소 1명 리뷰 필요
- 모든 테스트 통과
- 린트 에러 없음
- `tests/scratch/` 비어있을 것

---

## 참고 문서

- [아키텍처 설계](docs/architecture/README.md)
- [API 문서](docs/api/README.md)
- [개발 가이드](docs/guides/development/README.md)

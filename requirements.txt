click>=8.1.0
requests==2.32.5
pykrx>=1.0.0  # KRX 데이터 조회 (종목 리스트, 시세)
tenacity>=8.2.0
pydantic>=2.0.0
websockets==12.0
pandas==2.3.3
numpy==1.26.3
sqlalchemy==2.0.45
python-dotenv==1.0.0
schedule==1.2.1
ta==0.11.0  # Technical Analysis library
pytest==7.4.4
black==25.11.0
pylint==3.0.3
matplotlib==3.8.2
seaborn==0.13.2
plotly==5.18.0
scipy==1.13.1
pyarrow==15.0.0  # pandas 3.0 대비
psutil>=5.9.0  # 시스템 메트릭 (P2-3)
aiohttp>=3.9.0  # 비동기 HTTP 클라이언트 (P2-4)

# 데이터베이스 (PostgreSQL 지원)
psycopg2-binary>=2.9.9  # PostgreSQL 드라이버

# 캐시 (Redis)
redis>=5.0.0  # Redis 클라이언트

# P3 선택적 의존성 (고급 기능)
# torch>=2.0.0  # LSTM 예측 (P3-1) - 크기가 커서 주석 처리, 필요시 설치
# gymnasium>=0.29.0  # 강화학습 환경 (P3-2) - 선택적
# stable-baselines3>=2.0.0  # PPO 에이전트 (P3-2) - 선택적
# =====================================================
# 한투 퀀트 환경 설정 파일 예제
# 이 파일을 .env로 복사하고 실제 값을 입력하세요
# =====================================================

# 한국투자증권 API 설정
APP_KEY="your_app_key_here"
APP_SECRET="your_app_secret_here"
ACCOUNT_NUMBER="your_account_number_here"
ACCOUNT_PROD_CODE="01"

# 서버 설정 (virtual: 모의투자, prod: 실전투자)
SERVER="virtual"

# Telegram 알림 설정
TELEGRAM_BOT_TOKEN="your_telegram_bot_token_here"
TELEGRAM_CHAT_ID="your_chat_id_here"

# API 서버 보안 설정 (프로덕션에서는 필수!)
# 강력한 랜덤 문자열 사용 권장 (예: openssl rand -hex 32)
API_SERVER_KEY=""

# API 서버 네트워크 설정
# 보안: 프로덕션에서는 127.0.0.1 사용 권장, 외부 접근 필요시 리버스 프록시 사용
API_HOST="127.0.0.1"
API_PORT="8000"

# =====================================================
# 데이터베이스 설정 (Docker 환경)
# =====================================================
# PostgreSQL 설정 (docker-compose에서 자동 설정됨)
DB_USER="hantu"
# DB_PASSWORD는 더 이상 사용하지 않습니다 - ~/.pgpass 파일을 사용하세요
# DB_PASSWORD="hantu_secret_change_me"

# PostgreSQL 인증: ~/.pgpass 파일 사용 (권장)
# 파일 생성: echo "localhost:5432:hantu_quant:hantu:your_password" > ~/.pgpass
# 권한 설정: chmod 600 ~/.pgpass
# 로컬 개발 (SSH 터널): localhost:15432:hantu_quant:hantu:your_password
# 서버 환경: localhost:5432:hantu_quant:hantu:your_password
# 자세한 내용: deploy/PGPASS_MIGRATION_GUIDE.md 참조
DB_NAME="hantu_quant"
# DATABASE_URL은 docker-compose에서 자동 생성됨
# 로컬 개발 시 설정하지 않으면 SQLite 사용

# Redis 설정 (캐시, 선택사항)
# REDIS_URL="redis://localhost:6379/0"

# =====================================================
# 로깅 설정
# =====================================================
LOG_LEVEL="INFO"

# =====================================================
# 주의사항:
# 1. 이 파일은 Git에 커밋됩니다 (.env.example)
# 2. 실제 값이 포함된 .env 파일은 절대 커밋하지 마세요
# 3. API 키는 정기적으로 로테이션 권장
# 4. 프로덕션 환경에서는 API_SERVER_KEY 설정 필수
# 5. DB_PASSWORD는 반드시 강력한 비밀번호로 변경하세요
# ===================================================== 
# DATABASE_URL - PostgreSQL 연결 문자열
# ⚠️ 중요: 이 값을 설정하면 settings.py의 자동 감지를 오버라이드합니다!
#
# 우선순위:
#   1. DATABASE_URL 환경변수 (최우선 - 자동 감지 무시!)
#   2. HANTU_ENV 환경변수 (local/server/test)
#   3. 경로 기반 자동 감지 (/Users/*, /opt/*, /home/ubuntu)
#
# 권장: 이 항목을 설정하지 말고 자동 감지 사용
#
# 로컬 개발 (SSH 터널): localhost:15432
# 서버 환경 (직접 연결): localhost:5432
#
# 명시적 설정이 필요한 경우:
# DATABASE_URL=postgresql://hantu:YOUR_PASSWORD@localhost:15432/hantu_quant  # 로컬
# DATABASE_URL=postgresql://hantu:YOUR_PASSWORD@localhost:5432/hantu_quant   # 서버

# 또는 HANTU_ENV로 환경 지정 (권장)
# HANTU_ENV=local   # 자동으로 localhost:15432 사용
# HANTU_ENV=server  # 자동으로 localhost:5432 사용
# HANTU_ENV=test    # SQLite 사용
